import { Given, When, Then } from "@badeball/cypress-cucumber-preprocessor";

/* No duplicate steps, this one already in lediga-plats-specifik.js
Given('that the user is on the homepage', () => {});*/

When('the user presses on the header {string}', (a) => {
  // TODO: implement step
});

/* No duplicate steps, this one already in lediga-plats-specifik.js
When('the user chooses a movie', () => {});*/

Then('the user should be able to see details about the specific movie of choice', () => {
  // TODO: implement step
});