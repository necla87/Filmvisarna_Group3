import { Given, When, Then } from "@badeball/cypress-cucumber-preprocessor";

/* No duplicate steps, this one already in filtera-filmer.js
Given('that the customer visit the webbsite', () => {});*/

When('I click on {string}', (a) => {
  // TODO: implement step
});

Then('it shows the trailer of the movies that I have chosen to watch', () => {
  // TODO: implement step
});